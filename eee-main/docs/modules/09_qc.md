# Quality Control 模块 (QC)

> 文件: [src/pages/qc/QualityControlModule.tsx](../../src/pages/qc/QualityControlModule.tsx)
> 路由: `activeModule === 'qc'` (在 [App.tsx](../../src/App.tsx) 中分支渲染)
> 状态: ✅ Phase 1 MVP 完成,移植自 `qc-demo/`(原独立 FastAPI + React 应用)

---

## 术语映射(UI ↔ Data 模型)

| UI 显示 | 数据库 / 代码 | 含义 |
|---------|--------------|------|
| **Dry Room** (M-037 起) | 物理 dryer(1..5),由 `qc_drying_location.dryer_number` 表达 | 5 个物理烘干房,每个 100 cell(10×10),cell 编号 00..99 |
| **Batches** | `qc_production_lot` | 生产批 / 工单,下属多 sub-lot |
| Drying sub-lot / Sub-lot | `qc_drying_sub_lot` | 一车货,实际取样检验的单位 |
| **Testing** | screen key `pending` | 待检队列页(子批状态机里的 pending/inspecting) |
| **Production** | screen key `production` | 临时创建表单,一次出 1 个 Batch + N 个 sub-lot,sub-lot 落 `created` 状态 |
| Cell / Spot | `qc_drying_location` 单行 | 单个烘干位,被 sub-lot 通过 `location_id` 占用 |

> **历史**:M-035 时 UI 把 production_lot 显示成 "Dry Room",M-036 引入物理 dryer 网格,M-037 把侧边栏的 "Dry Room" 让给物理 dryer 视角,production_lot 列表退到 "Batches" 入口。schema 没动,只是显示层和路由重新组织。

---

## 概述

宠物零食制造业的"**烘干后 QC 闭环**"模块,核心解决[QC模块起步与设计指南](../../../docs/QC模块起步与设计指南.md) §1.4 描述的:
> 中午烘干完、傍晚才收到结果 → 不合格品堆积。

闭环流程:
```
生产批 → 进烘干房(check_in)
       → 出烘干房(check_out → pending)
       → QC 员录入 Aw(submit_inspection → passed / hold)
       → 管理员处置 Hold(create_disposition → closed)
```

模块用同一套子批模型 + 状态机 + 事件流,覆盖待检看板、批次追溯、Hold 处置、SKU/模板维护等场景。

---

## 子页面 / Screen

QC 模块用 `screen` state 切换页面(沿用 HR 模块的模式)。

| `screen` 值 | 组件 | 文件 | 权限 |
|------------|------|------|------|
| `home` | `QcHome` (M-041 合并版) | [QcHome.tsx](../../src/pages/qc/QcHome.tsx) | **QC Home + Dashboard 二合一**:顶部 6 张 stat 卡(expected_finish_today / currently_drying / room_temp_drying / awaiting_sample / awaiting_wa_result / passed_today・failed_today・pass_rate),下方 "Needs attention" 24h 内 pass/fail 卡片(Pass → Release 按钮,Fail → Dispose 跳 Testing),底部 4 个 Quick links |
| `production` | `Production` | [Production.tsx](../../src/pages/qc/Production.tsx) | `qc.production_lots.create` + `qc.sub_lots.check_in` |
| `dry-rooms` | `DryRoomsList` | [DryRoomsList.tsx](../../src/pages/qc/DryRoomsList.tsx) | 5 个物理 dryer 卡片(占用 / 可用 / 下一个出烘干时间) |
| `dry-room-detail` | `DryRoomDetail` | [DryRoomDetail.tsx](../../src/pages/qc/DryRoomDetail.tsx) | 左 grid + 右"待入烘干" + "在烘干"(按剩余时间升序,带 check-out) |
| ~~`batches`~~ | ~~`LotsList`~~ | _M-042 起从侧边栏移除_ — Production 表单是创建 Batch 的唯一入口,文件保留作 dead code |
| ~~`lot-detail`~~ | ~~`LotDetail`~~ | _M-042 起从侧边栏不可达_ — 文件保留 |
| `testing` | `TestingPage` | [TestingPage.tsx](../../src/pages/qc/TestingPage.tsx) | M-039 新版,取样 → WA(实时自动判定)→ 直接 Confirm PASS/FAIL · 或 Redo test 清掉重测;Fail 后 DispositionPicker 分流 |
| `room-temp` | `RoomTempDryPage` | [RoomTempDryPage.tsx](../../src/pages/qc/RoomTempDryPage.tsx) | 从 Dry Rooms 列表的 Room Temp Dry 卡片进入(非顶级侧边栏入口);count-up 计时,Stop 后 sub-lot 回 `pending` |
| `pending` / `inspect` | `PendingQueue` / `InspectPage` (legacy) | 旧 testing 流程,M-039 后仍保留路径方便回退 |
| `pending` | `PendingQueue` (UI 显示 **Testing**) | [PendingQueue.tsx](../../src/pages/qc/PendingQueue.tsx) | `qc.inspections.view`(批量提交需 `inspections.submit`) |
| `inspect` | `InspectPage` | [InspectPage.tsx](../../src/pages/qc/InspectPage.tsx) | `qc.inspections.submit` |
| `dashboard` | `AdminDashboard` | [AdminDashboard.tsx](../../src/pages/qc/AdminDashboard.tsx) | `qc.dashboard.view`(批量处置需 `dispositions.create`) |
| `trace` | `TraceListPage` | [TraceListPage.tsx](../../src/pages/qc/TraceListPage.tsx) | `qc.trace.view` |
| `trace-detail` | `TracePage` | [TracePage.tsx](../../src/pages/qc/TracePage.tsx) | `qc.trace.view` |
| `products` | `ProductManagement` | [ProductManagement.tsx](../../src/pages/qc/ProductManagement.tsx) | `qc.products.view`(增删改需对应权限) |

---

## "全选" UI 控件

按本次任务要求,模块内**所有列表页**和**权限管理页**都加了批量选择/操作能力。

| 页面 | 全选范围 | 批量动作 |
|------|---------|---------|
| `LotsList`(Dry Rooms) | 全部生产批 | 批量删除(`canDelete`) |
| `LotDetail` | 状态 = drying 的子批 | 批量 check-out(共用同一个 check-out 时间) |
| `PendingQueue`(Testing) | 全部 pending/inspecting 子批 | 同一 Aw 批量提交(QC 流水线一致读数场景) |
| `AdminDashboard` (Hold 面板) | 全部 hold 子批 | 同一处置类型批量处置(含 remark) |
| `ProductManagement` | 全部 SKU | 批量删除 |
| `TraceListPage` | 全部生产批 | 批量导出 CSV |
| `UserDetail`(权限管理) | 模块开关 / 模块内全部权限 / 单 resource 全部动作 | 一键启用 / 关闭(参见 [auth/UserDetail.tsx](../../src/pages/auth/UserDetail.tsx)) |

`SelectAllCheckbox` 公共组件: [components/SelectAllCheckbox.tsx](../../src/pages/qc/components/SelectAllCheckbox.tsx),支持 indeterminate 状态(部分选中)。

---

## Production 临时创建页

`Production` 页面是 QC 模块内的**临时创建向导**(因为还没有独立的 Production 模块)。一次提交同时创建 1 个 Dry Room(production_lot) + N 个 Sub-lot。

**字段**(顶部 production header):
- production_date(默认今天)
- shift(early / late / night / other)
- product SKU
- work_order_barcode
- production_code(默认按 `P-{YYYYMMDD}-{SHIFT}-{SKU}` 自动生成,可手动覆盖)

**Sub-lot 列表**(下半部分):
- 每行只需要 Sub-lot 编号(留空则自动生成 `<production_code>-D##`)
- "Add sub-lot" 可加多行,"Remove" 按钮删行
- 烘干位置和 check-in 时间**不在此处录入** —— 实际入烘干房时再录(在 Dry Room 详情页 check-in)

**实现**:`createProductionBatch` (qcApi.ts) 在前端层串行调用 `createProductionLot` + 多次 `qc_check_in_sub_lot` RPC,没有引入新表。Sub-lot 创建出来的初始 status 是 `drying`,location_id 为 NULL,in_time 默认 `now()`。

---

## Dry Rooms 页面包含什么

`DryRoomsList`(侧边栏 "Dry Rooms")现在列出 6 张卡片:

1. **Room Temp Dry**(橙色边)— 显示当前在室温干的车数 + 最长 elapsed,点击进入 `RoomTempDryPage`
2. **Dryer 1**~**Dryer 5** — 各显示占用率胶囊 + Occupied/Available 数 + 下一个出烘干时间 + 进度条;点击进入 `DryRoomDetail`(grid + 右侧 sub-lot 面板)

> Room Temp Dry **不**再是独立的侧边栏入口,而是 Dry Rooms 列表里的一张并列卡片(从车间视角看,室温干和烘干房都是"烘干装备的一种")。

---

## "Awaiting sample" 子状态(M-041)

在 `pending` 状态里区分两种实际场景:

| UI 显示 | 后端判定 | 含义 |
|---------|---------|------|
| **Awaiting sample**(沙漏 icon · 灰色 chip) | `status='pending' AND NOT has_pending_sample` | 子批已出烘干房,但还没人取样 |
| **Sample S-XXX · awaiting WA**(烧杯 icon · 蓝色 chip) | `status='pending' AND has_pending_sample` 或 `status='inspecting'` | 已取样,等录 Aw 读数 |

`has_pending_sample` 是 `qc_sub_lot_to_json` 的派生字段(M-041),前端不用再额外查询。

---

## Testing 简化流程(用户反馈调整)

原本的 "Preview judgment" 中间步骤已经去掉。新流程:

1. 取样:输 sample_id → 点 `取样`
2. Measure:数字键盘录 WA → 顶部读数框**实时**显示绿/红 + "Within spec — Pass" / "Out of spec — Fail" 文案
3. 底部固定两个按钮:
   - **Redo test** — 清空当前 WA 读数(sample 不变,可再录一次)
   - **Confirm PASS/FAIL & persist** — 颜色随 judged 结果变(绿/红),WA 未录时为 "Enter a reading first" 灰态

Pass → 进 `done` step(绿色 PASS · released);Fail → 弹 DispositionPicker。

---

## Check-in to Dryer 网格页

[`CheckInToDryer.tsx`](../../src/pages/qc/CheckInToDryer.tsx) 是 Production 之后的物理放置环节。

- 左栏:状态为 `created` 的 sub-lot 列表(等待入烘干房)
- 右栏:5 个 dryer 标签,每个 10×10 网格,格子编号 **00 ~ 99**(行优先,`row * 10 + col`)
- 颜色:灰 = 未选 sub-lot,绿 = 可用,蓝 = 选中,红 = 已被其他 active sub-lot 占用
- 操作:点击 sub-lot → 点击格子 → "Confirm check-in" → 后端 `qc_register_in_dryer` 把状态从 `created` 切到 `drying`,写入 `location_id` + `in_time` + `check_in` 事件
- 占用判定:`status ∈ (drying / pending / inspecting / hold / disposing)` 的 sub-lot 锁住格子;`passed` / `closed` / `created` 不锁

---

## 状态机

```
(null) ── create_sub_lot ──→ created          -- Production 创建,不在烘干房
created ── register_in_dryer ──→ drying       -- Dry Room 详情页 awaiting check-in
(null) ── register_in ──→ drying              -- LotDetail 直接 check-in(legacy 路径,跳过 created)
drying ── move (target empty) ──→ drying      -- 自己换格子
drying ── move (target occupied)
       → 自己进新格 (drying),被挤的进 awaiting_recheck
awaiting_recheck ── register_in_dryer ──→ drying  -- 重新放回任意空格,累计时长续算
drying ── register_out ──→ pending
pending ── take_sample ──→ pending          -- 取样不改状态,创建 sample 记录
pending ── submit_inspection (pass) ──→ passed
pending ── submit_inspection (fail) ──→ hold
hold ── disposition (redry_dryer) ──→ awaiting_recheck  -- 新 expected_dry_minutes
hold ── disposition (room_temp_dry) ──→ room_temp_drying (open session)
hold ── disposition (scrap/concession/...) ──→ closed
room_temp_drying ── stop_room_temp_dry ──→ pending  -- 回到 Testing 队列重新取样测
pending ── start_inspection ──→ inspecting
inspecting ── submit_pass ──→ passed
inspecting ── submit_fail ──→ hold
hold ── start_disposition ──→ disposing
disposing ── complete_disposition ──→ closed
```

`qc_drying_sub_lot.status` 的 CHECK 约束限制了取值集合;转移逻辑则强制在 SQL RPC 函数里(`qc_check_in_sub_lot` / `qc_check_out_sub_lot` / `qc_submit_inspection` / `qc_create_disposition`)。前端**不**自行做状态转移,只调 RPC。

事件流写入 `qc_quality_event`,事件类型:`check_in`、`check_out`、`inspection_passed`、`inspection_failed_hold`、`disposition_completed`。

---

## 业务规则 (BR)

| 编号 | 内容 | 实现位置 |
|------|------|---------|
| **BR-Q1** | Aw 合格判定为闭区间 `[lower_limit, upper_limit]`,超出范围即 fail。 | `qc_submit_inspection` (M-034) |
| **BR-Q2** | 子批状态机不允许跨步转移,例如 drying 不能直接 → passed,会抛 `Sub-lot not inspectable` 异常。 | `qc_submit_inspection` / `qc_check_out_sub_lot` / `qc_create_disposition` |
| **BR-Q3** | submit_inspection 时若状态为 `pending`,函数自动先转 `inspecting` 再判定(对应 qc-demo 的 "start_inspection")。 | `qc_submit_inspection` |
| **BR-Q4** | 每个 SKU 当前默认对应一个检验模板(`LIMIT 1`);多模板支持留待 Phase 2。 | `qc_submit_inspection` / `qc_list_products` |
| **BR-Q5** | 子批 check-in 自动生成 `<lot_barcode>-D##` 编号,序号按该生产批已有子批数 + 1。可显式覆盖。 | `qc_check_in_sub_lot` |
| **BR-Q6** | dispositon `type` 限定 `rework / grind / scrap / concession`(qc-demo 沿用)。 | `qc_create_disposition` CHECK |
| **BR-Q7** | dashboard `pass_rate` 取今日 inspection_record `pass / (pass+fail)` 四舍五入到 1 位小数;无数据返回 NULL。 | `qc_dashboard_summary` |

---

## 数据源 / API

前端层 [src/services/qcApi.ts](../../src/services/qcApi.ts) 封装所有 Supabase 调用。主要分组:

| 分组 | 函数 | 底层调用 |
|------|------|---------|
| **Master** | `listProducts`、`listLocations` | `qc_list_products` / `qc_list_locations` RPC |
| **Master CRUD** | `createProduct`、`updateProduct`、`deleteProduct`、`deleteProducts` | 直查 `qc_product_sku` + `qc_inspection_template` |
| **Lot 列表** | `listProductionLots`、`createProductionLot`、`deleteProductionLots`、`productionLotDetail` | RPC + 直查 |
| **Sub-lot 操作** | `checkInSubLot`、`checkOutSubLot`、`checkOutSubLotsBulk` | `qc_check_in_sub_lot` / `qc_check_out_sub_lot` |
| **检验** | `listPendingInspections`、`inspectionTemplateForSubLot`、`submitInspection`、`submitInspectionsBulk` | `qc_list_pending_inspections` / `qc_submit_inspection` |
| **处置** | `createDisposition`、`createDispositionsBulk` | `qc_create_disposition` |
| **仪表盘** | `dashboardSummary` | `qc_dashboard_summary` |
| **演示数据** | `seedDemoData` | `qc_seed_demo_data` |

所有"bulk"函数都是前端层 for-loop 顺序调用单条 RPC,Phase 2 可考虑专用批量 RPC。

---

## TypeScript 类型

主要类型在 [src/services/qcApi.ts](../../src/services/qcApi.ts) 顶部声明:

```ts
type SubLotStatus = 'drying' | 'pending' | 'inspecting' | 'passed' | 'hold' | 'disposing' | 'closed';
interface SubLot { /* id, sub_lot_code, status, in_time, out_time, wait_minutes, hold_*, ... */ }
interface ProductionLot { /* lot_number, lot_barcode, sku_id, sku_name, ... */ }
interface Product { id, code, name, standard_drying_minutes, templates: InspectionTemplate[] }
interface DashboardSummary { pending_count, hold_count, pass_rate, pending_items, holds, today_*_items }
interface QualityEvent { event_type, payload, summary, created_at, sub_lot_code }
```

UI 颜色映射 `STATUS_COLOR` + 文案 `STATUS_LABEL` 也在该文件下方,沿用 qc-demo 原配色(drying=sky / pending=amber / passed=emerald / hold=red / disposing=purple / closed=slate)。

---

## 数据库表

详见 [03_migrations-and-edge-functions.md M-033](../database/03_migrations-and-edge-functions.md#m-033-20260520000001_qc_initial_schemasql)。

| 表 | 关键字段 | 说明 |
|---|---|---|
| `qc_product_sku` | code, name, standard_drying_minutes | SKU 主数据 |
| `qc_inspection_template` | sku_id, item_name, lower_limit, upper_limit | 检验模板(每 SKU 一份) |
| `qc_drying_location` | code, display_name | 烘干位置/层架 |
| `qc_production_lot` | lot_number, lot_barcode, work_order_barcode, sku_id | 生产批 |
| `qc_drying_sub_lot` | sub_lot_code, production_lot_id, location_id, in_time, out_time, status | 烘干子批(状态机所在) |
| `qc_inspection_record` | drying_sub_lot_id, inspector_auth_id, values_json, result | 单次检验记录 |
| `qc_disposition` | drying_sub_lot_id, type, remark, operator_auth_id | Hold 处置 |
| `qc_quality_event` | drying_sub_lot_id, event_type, payload, actor_auth_id | 事件流(追溯/审计) |

所有 actor / inspector / operator 字段均指向 `auth.users.id`,**不**单独维护 qc-demo 原有的 `qc.app_user` 表。

---

## 权限矩阵(M-040 起细粒度版)

权限定义见 [src/lib/permissionStructure.ts](../../src/lib/permissionStructure.ts) `qc` 段。每种动作一个独立开关(BR-Q19)。

| Resource | Permission | 谁用 / 用在哪 |
|----------|-----------|---------------|
| `module_permissions` | manage | 模块管理员(在 UserDetail 编辑他人 QC 权限) |
| **production** | create_batch | Production 页(线长 / 生产) |
| **batches** | view / create / delete | Batches 列表页(管理员) |
| **dry_rooms** | view_status | Dry Rooms 列表 + 详情(所有现场角色) |
| dry_rooms | check_in | DryRoomDetail 把 sub-lot 放进格子(烘干房操作工) |
| dry_rooms | move | DryRoomDetail Move-to-other-spot(烘干房操作工 / 主管) |
| dry_rooms | check_out | DryRoomDetail Check out(烘干房操作工) |
| **testing** | view_status | Testing 页 + Room Temp Dry 页(QC 员 / 管理层) |
| testing | take_sample | TestingPage 取样按钮(QC 员) |
| testing | submit_inspection | TestingPage Confirm 提交 WA(QC 员) |
| testing | dispose_redry | DispositionPicker 选 Re-dry in dryer(质量经理) |
| testing | dispose_room_temp | DispositionPicker 选 Room temp dry(质量经理) |
| testing | dispose_scrap_concession | DispositionPicker 选 Scrap / Concession 等(质量经理) |
| testing | stop_room_temp | RoomTempDryPage Stop 按钮(室温干操作工) |
| **dashboard** | view | 看 QC Home / Dashboard 合并页(M-041 合并后) |
| dashboard | release_pass | QC Home 上 Pass 行的 Release 按钮(把 passed → closed,assign next step) |
| **sub_lots** | view_history | 任何页打开 SubLotHistoryDrawer(审计 / QA) |
| products | view / create / edit / delete | ProductManagement(质量部) |
| locations | view / manage | 烘干位置主数据(目前 500 个 cell 是固定 seed) |
| dashboard | view | 管理仪表盘 |
| trace | view | Batch trace(可点 sub-lot 跳 history) |
| audit_log | view | QC 审计 |

旧权限种子(M-035 coarse 版)已被 M-040 完全替换,旧键 `production_lots.*` / `sub_lots.check_in` / `inspections.submit` / `dispositions.create` 不再使用。

---

## qc-demo 与主 ERP 的差异

| 维度 | qc-demo(独立 app) | 主 ERP 集成版 |
|------|--------------------|---------------|
| 后端 | FastAPI(Python) + SQLAlchemy | Supabase RPC(PL/pgSQL) |
| 路由 | react-router-dom | App.tsx state machine + 模块内 `screen` state |
| 认证 | qc.app_user + JWT | Supabase Auth + erp_user + user_permission_grant |
| 角色 | role 列(qc / manager) | 由权限组合表达 |
| schema | `qc.*`(独立 schema) | `public.qc_*`(沿用 hr_* 约定) |
| 全选 | 无 | 所有列表 + 权限管理页加 SelectAllCheckbox + 批量动作 |

---

## 待开发(Phase 2+)

- 真正的批量 RPC(目前 bulk 都是前端循环单条)
- 检验模板支持多个检验项(目前每 SKU 取 LIMIT 1)
- QC 审计日志页(权限点已声明 `qc.audit_log.view`,页面待实现)
- 与库存模块联动:passed 子批自动转入下道工序、hold 子批锁定库存
- 仪器对接(蓝牙 / 网络仪表),目前是人工读数 + 大数字键盘
- 烘干炉档期 / 排队看板(便于返烘决策)
